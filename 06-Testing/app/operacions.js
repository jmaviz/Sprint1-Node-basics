function suma(a, b) {
    return a + b;
  }
 
  function resta(a, b) {
    return a - b;
  }

  function multiplicacio(a, b) {
    return a * b;
  }

  function divisio(a, b) {
    return a / b;
  }

  module.exports = {
    suma, resta, multiplicacio, divisio}